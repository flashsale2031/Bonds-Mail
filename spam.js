(()=>{let selected=null;const esc=s=>(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function render(){const a=BondsMail.load().messages.filter(m=>m.folder==='spam').sort((x,y)=>new Date(y.date)-new Date(x.date));document.getElementById('messageList').innerHTML=a.length?a.map(m=>'<article class="message '+(selected===m.id?'selected':'')+'" data-id="'+m.id+'"><span>⚠</span><div><div class="message-subject">'+esc(m.from||'Me')+' · '+esc(m.subject)+'</div><div class="message-preview">'+esc(m.body)+'</div></div><span class="message-time">'+new Date(m.date).toLocaleDateString()+'</span></article>').join(''):'<div class="empty">No messages in this folder.</div>';document.querySelectorAll('.message').forEach(x=>x.onclick=()=>open(x.dataset.id))}function open(id){selected=id;const m=BondsMail.load().messages.find(x=>x.id===id);document.getElementById('readingPane').innerHTML='<div class="reading"><h2>'+esc(m.subject)+'</h2><div class="meta">'+esc(m.from||'Me')+(m.to?' → '+esc(m.to):'')+'<br>'+new Date(m.date).toLocaleString()+'</div><div class="body">'+esc(m.body)+'</div></div>';render()}render()})();